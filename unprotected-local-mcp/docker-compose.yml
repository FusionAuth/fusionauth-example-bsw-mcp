services:

  mcp-server:
    build:
      context: ./mcp-server
    environment:
      MCP_SERVER_URL: http://localhost:8000
    ports:
      - "8000:8000"
    networks:
      - fusionauth

volumes:
  db_data:
  fusionauth_config:

networks:
  fusionauth:
    driver: bridge
